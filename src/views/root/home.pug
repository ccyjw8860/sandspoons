extends base.pug
include ../mixins/video

block content 
    h2 Welecome here you will see the trending videos

    each video in videos 
        +videoVisual(video)
    else 
        li Sorry nothing found.

block scripts
    script(src="/assets/js/main.js")
