extends ../root/base
include ../mixins/video

block content 
    if errorMessage 
        span=errorMessage 
    h1=user.username

    each video in videos 
        +videoVisual(video)
    else 
        li Sorry nothing found.
block scripts
    script(src="/assets/js/main.js")